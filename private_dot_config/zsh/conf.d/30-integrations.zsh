# ========================================
# External Integrations
# ========================================

# GPG Configuration
# GPG_TTYは対話シェルでのみ設定（非対話シェルでのエラー回避）
if [[ -t 1 ]]; then
  export GPG_TTY="$(tty)"
fi

# ========================================
# Rust
# ========================================
. "$HOME/.local/share/cargo/env"

# ========================================
# Zoxide
# ========================================
eval "$(zoxide init zsh)"
